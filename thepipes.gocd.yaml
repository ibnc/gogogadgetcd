format_version: 2
common:
  plan: &plan
    clean_workspace: true
    jobs:
      plan:
        tasks:
        - script: |
            echo "PLAN!"
  apply: &apply
    approval:
      type: manual
    clean_workspace: true
    jobs:
      apply:
        tasks:
        - script: |
            echo "configing APPLY!"
  # materials: &materials
  #   thing:
  #     type: configrepo
pipelines:
  AAAA:
    group: GOgogadgetCD
    materials:
      thing1:
        type: configrepo
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
  BBBB:
    group: GOgogadgetCD
    materials:
      thing2:
        type: configrepo
      parent:
        pipeline: AAAA
        stage: apply
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
  CCCC:
    group: GOgogadgetCD
    materials:
      thing3:
        type: configrepo
      parentTwo:
        pipeline: AAAA
        stage: apply
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
