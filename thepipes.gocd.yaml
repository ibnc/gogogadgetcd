format_version: 2
common:
  plan: &plan
    clean_workspace: true
    jobs:
      plan:
        tasks:
        - script: |
            echo "PLAN!"
  apply: &apply
    approval:
      type: manual
    clean_workspace: true
    jobs:
      apply:
        tasks:
        - script: |
            echo "APPLY!"
  # materials: &materials
  #   thing:
  #     type: configrepo
pipelines:
  AA:
    group: GOgogadgetCD
    materials:
      thing1:
        type: configrepo
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
  B:
    group: GOgogadgetCD
    materials:
      thing2:
        type: configrepo
      parent:
        pipeline: AA
        stage: apply
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
  C:
    group: GOgogadgetCD
    materials:
      thing3:
        type: configrepo
      parentTwo:
        pipeline: AA
        stage: apply
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
