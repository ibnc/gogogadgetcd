format_version: 2
common:
  plan: &plan
    clean_workspace: true
    jobs:
      plan:
        tasks:
        - script: |
            echo "PLAN!"
  apply: &apply
    approval:
      type: manual
    clean_workspace: true
    jobs:
      apply:
        tasks:
        - script: |
            echo "APPLY!"
  # materials: &materials
  #   thing:
  #     type: configrepo
pipelines:
  H:
    group: GOgogadgetCD
    materials:
      thing1:
        type: configrepo
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
  V:
    group: GOgogadgetCD
    materials:
      thing2:
        type: configrepo
      parent:
        pipeline: H
        stage: apply
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
  W:
    group: GOgogadgetCD
    materials:
      thing3:
        type: configrepo
      parentTwo:
        pipeline: H
        stage: apply
    stages:
      - plan:
          <<: *plan
      - apply:
          <<: *apply
